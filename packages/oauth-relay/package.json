{
  "dependencies": {},
  "description": "Minimalistický OAuth bounce relay pro Hertz — přesměruje autorizační kód z veřejné URL zpět na instanci na privátní IP. Žádné závislosti.",
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=22"
  },
  "files": [
    "dist",
    "Dockerfile",
    "README.md"
  ],
  "license": "Apache-2.0",
  "main": "./dist/index.js",
  "name": "@kuclab-hertz/oauth-relay",
  "repository": {
    "directory": "packages/oauth-relay",
    "type": "git",
    "url": "git+https://github.com/Jerry256254/Hertz.git"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist *.tsbuildinfo",
    "start": "node dist/index.js",
    "test": "node --test test/*.test.mjs",
    "typecheck": "tsc -b --noEmit"
  },
  "type": "module",
  "types": "./dist/index.d.ts",
  "version": "0.1.0"
}
